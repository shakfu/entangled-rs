[package]
name = "pyentangled"
version = "0.2.0"
edition = "2024"
description = "Python bindings for Entangled literate programming engine"
license = "MIT"

[lib]
name = "_core"
crate-type = ["cdylib"]

[dependencies]
entangled = { path = "../entangled", version = "0.2.0" }
pyo3 = { version = "0.29", features = ["extension-module", "abi3-py39"] }
